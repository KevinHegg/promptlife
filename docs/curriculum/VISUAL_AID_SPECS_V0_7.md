# Visual Aid Specs v0.7

| # | visualAidId | Lesson | Learning objective | What learner sees | Touch/rotate/toggle | 2D version | 3D version if justified | Reduced-motion fallback | Accessibility description | Complexity | Decision |
|---:|---|---|---|---|---|---|---|---|---|---|---|
| 1 | v07-what-is-llm | What Is an LLM? | Fluent output means consciousness or database lookup. | Prompt lifecycle overview with context, fixed weights, logits, sampling, and append-repeat. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Prompt lifecycle overview with context, fixed weights, logits, sampling, and append-repeat. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: A large language model is a learned system that predicts likely next tokens from the context it can see. | low | build now |
| 2 | v07-rationalists-empiricists | Rationalists vs Empiricists | Deep learning is anti-reason or pure magic. | Split-screen contrast of principle path and experience path feeding into AI history. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Split-screen contrast of principle path and experience path feeding into AI history. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Rationalist approaches emphasize rules and reasoning from principles; empiricist approaches emphasize learning from experience and evidence. | low | later |
| 3 | v07-symbolic-vs-deep-learning | Symbolic AI vs Deep Learning | Symbolic AI is obsolete, or deep learning is magic. | Rule cards on one side, learned landscape on the other, with a hybrid product layer above both. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Rule cards on one side, learned landscape on the other, with a hybrid product layer above both. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Symbolic AI manipulates explicit symbols and rules; deep learning adjusts many weights until useful patterns are captured. | low | build now |
| 4 | v07-training | Training | Every chat message trains the model. | Predict, compare, loss, update weights, repeat. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Predict, compare, loss, update weights, repeat. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Training updates model weights by comparing predictions with targets or preferences and adjusting parameters. | low | build now |
| 5 | v07-pretraining | Pretraining | Pretraining stores a perfect copy of every document. | Data rain shaping a terrain with broad paths. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Data rain shaping a terrain with broad paths. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Pretraining is broad early training that builds general capability through many durable weight updates. | low | build now |
| 6 | v07-overfitting-generalization | Overfitting and Generalization | Bigger training always means better understanding. | A path that perfectly traces old dots but misses new dots. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: A path that perfectly traces old dots but misses new dots. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Generalization means learned patterns work on new examples; overfitting means the model clings too closely to training examples or brittle patterns. | medium | later |
| 7 | v07-fine-tuning | Fine-Tuning | Fine-tuning is just a better prompt. | Targeted trail over a pretrained landscape. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Targeted trail over a pretrained landscape. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Fine-tuning is targeted additional training after broad pretraining. | low | build now |
| 8 | v07-alignment | Alignment | Alignment means the model is morally good or understands ethics. | Response landscape with preferred paths, warning zones, and policy rails. | Sort/check cards by misconception and correction. | Mobile SVG or HTML/SVG hybrid: Response landscape with preferred paths, warning zones, and policy rails. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Alignment is the effort to shape model behavior toward human goals, instructions, safety boundaries, and preferences. | medium | later |
| 9 | v07-inference | Inference | Inference trains the model. | Forward-pass pipeline with fixed weights below temporary activations. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Forward-pass pipeline with fixed weights below temporary activations. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Inference is normal model use: fixed weights process the current context to generate response tokens. | low | build now |
| 10 | v07-prompt-response | Prompt vs Response | The model writes the whole answer at once. | Prompt cards and generated response cards entering the current context together. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Prompt cards and generated response cards entering the current context together. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: The prompt is supplied input context; the response is generated tokens added after the model reads the context. | low | build now |
| 11 | v07-tokenization | Tokenization | A token is always a whole word. | Dog/cat sentence split into uneven token chunks. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Dog/cat sentence split into uneven token chunks. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Tokenization splits text into chunks the model can represent as token IDs. | low | build now |
| 12 | v07-token-ids | Token IDs | Token IDs are meanings or definitions. | Token chunk to ID to embedding row. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Token chunk to ID to embedding row. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: A token ID is the number assigned to a token so the model can look up its learned starting vector. | low | build now |
| 13 | v07-embeddings | Embeddings | Embeddings are definitions. | Embedding lookup table pulling a learned vector row. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Embedding lookup table pulling a learned vector row. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: An embedding is a token ID’s learned starting vector. | low | build now |
| 14 | v07-vectors | Vectors | Each vector dimension has a neat English meaning. | Feature bars with simplified labels and a warning that labels are approximations. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Feature bars with simplified labels and a warning that labels are approximations. | CSS 3D candidate: Feature bars with simplified labels and a warning that labels are approximations. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: A vector is a list of numbers that represents learned features of something, such as a token. | medium | build now |
| 15 | v07-tensors | Tensors | A tensor is just scary jargon for one number. | Token-by-feature block, with optional batch dimension as a deep note. | Rotate lightly only if 3D clarifies axes or depth. | Mobile SVG or HTML/SVG hybrid: Token-by-feature block, with optional batch dimension as a deep note. | CSS 3D candidate: Token-by-feature block, with optional batch dimension as a deep note. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: A tensor is an organized block of numbers, often holding many token vectors at once. | medium | build now |
| 16 | v07-context-window | Context Window | Context window equals permanent memory. | Transparent context tray with prompt, response-so-far, and retrieved cards; older cards fall out. | Push cards into the context tray and observe what is visible. | Mobile SVG or HTML/SVG hybrid: Transparent context tray with prompt, response-so-far, and retrieved cards; older cards fall out. | CSS 3D candidate: Transparent context tray with prompt, response-so-far, and retrieved cards; older cards fall out. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: The context window is the limited amount of tokens or media the model can currently use. | medium | build now |
| 17 | v07-attention | Attention | Attention means consciousness. | Weighted arcs between dog/cat sentence tokens. | Tap token nodes to reveal relevance arcs. | Mobile SVG or HTML/SVG hybrid: Weighted arcs between dog/cat sentence tokens. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Attention assigns weighted relevance between token positions. | low | build now |
| 18 | v07-mlp | MLP | The MLP does the same thing as attention. | Attention arrows feeding a per-token feature forge. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Attention arrows feeding a per-token feature forge. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: An MLP is a feed-forward network that reshapes each token position’s feature vector. | low | build now |
| 19 | v07-layers | Layers | Layers reveal a human-like reasoning trace. | Transparent layer stack with attention and MLP blocks repeated. | Rotate lightly only if 3D clarifies axes or depth. | Mobile SVG or HTML/SVG hybrid: Transparent layer stack with attention and MLP blocks repeated. | CSS 3D candidate: Transparent layer stack with attention and MLP blocks repeated. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Transformer layers are repeated blocks that refine hidden states, usually combining attention, MLPs, residual paths, and normalization. | medium | build now |
| 20 | v07-hidden-states | Hidden States | Hidden states are secret English or saved memories. | Same token glowing differently across contexts and layers. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Same token glowing differently across contexts and layers. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: A hidden state is the model’s temporary context-shaped vector for a token at a given layer. | low | build now |
| 21 | v07-logits | Logits | Logits are probabilities or truth confidence. | Raw candidate scoreboard. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Raw candidate scoreboard. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Logits are raw scores for candidate next tokens before probabilities. | low | build now |
| 22 | v07-softmax | Softmax | Softmax probabilities are truth confidence. | Score funnel into probability bars. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Score funnel into probability bars. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Softmax converts raw next-token scores into probabilities that sum to one. | low | build now |
| 23 | v07-sampling | Sampling | Sampling is meaningless randomness or deliberate choice. | Vocabulary cloud with candidate probability height and one selected token. | Toggle temperature/top-p and see the candidate cloud change. | Mobile SVG or HTML/SVG hybrid: Vocabulary cloud with candidate probability height and one selected token. | CSS 3D candidate: Vocabulary cloud with candidate probability height and one selected token. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Sampling chooses one next response token from the probability distribution. | medium | build now |
| 24 | v07-autoregression | Autoregression | The model writes the whole answer at once. | Loop: score, sample, append, run again. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Loop: score, sample, append, run again. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Autoregression generates text by repeatedly predicting the next token, appending it, and running again. | low | build now |
| 25 | v07-rag-retrieval | RAG and Retrieval | RAG means the model learned the document. | User question, retriever shelf, retrieved cards in context tray, generated response. | Push cards into the context tray and observe what is visible. | Mobile SVG or HTML/SVG hybrid: User question, retriever shelf, retrieved cards in context tray, generated response. | CSS 3D candidate: User question, retriever shelf, retrieved cards in context tray, generated response. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Retrieval-augmented generation retrieves outside information and places it into the model context before generating a response. | medium | build now |
| 26 | v07-grounding | Grounding | A citation-looking answer is automatically grounded. | Answer balloon tethered to evidence cards. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Answer balloon tethered to evidence cards. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Grounding connects a model response to available evidence, such as retrieved documents, tool results, data, or citations. | medium | later |
| 27 | v07-hallucinations | Hallucinations | Hallucination means lying or simple brokenness. | Polished answer from vocabulary cloud with missing evidence anchors. | Sort/check cards by misconception and correction. | Mobile SVG or HTML/SVG hybrid: Polished answer from vocabulary cloud with missing evidence anchors. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: A hallucination is a fluent model output that is unsupported, false, or fabricated. | low | build now |
| 28 | v07-prompt-injection-tool-risk | Prompt Injection and Tool Risk | Prompt injection is just a bad prompt or user error. | Trusted instruction lane, untrusted document lane, tool-permission gate. | Sort/check cards by misconception and correction. | Mobile SVG or HTML/SVG hybrid: Trusted instruction lane, untrusted document lane, tool-permission gate. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Prompt injection is when malicious or conflicting text in context tries to override intended instructions, especially dangerous when tools can act. | medium | later |
| 29 | v07-diffusion | Diffusion vs Autoregression | All generative AI works like text LLMs. | Side-by-side denoise sequence and next-token loop. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Side-by-side denoise sequence and next-token loop. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Diffusion generation usually starts from noise and denoises step by step, unlike autoregressive text generation. | low | later |
| 30 | v07-multimodal | Multimodal AI | Multimodal means human-like perception. | Text, image, audio, video lanes meeting in a representation hub. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Text, image, audio, video lanes meeting in a representation hub. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Multimodal AI can represent or process multiple media types such as text, images, audio, and video. | low | later |
| 31 | v07-effective-prompting | Effective Prompting From Model Literacy | Prompting is magic wording or permanent teaching. | Prompt components entering a context tray: task, role, constraints, examples, data, output format. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Prompt components entering a context tray: task, role, constraints, examples, data, output format. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Better prompts work because they shape the current context: task, constraints, examples, data, and output format. | low | build now |
| 32 | v07-energy-water-compute | Energy, Water, and Compute | Digital AI is cost-free. | Prompt on phone connected to data center, power grid, cooling loop, and network. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Prompt on phone connected to data center, power grid, cooling loop, and network. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: AI responses depend on physical infrastructure: chips, data centers, electricity, cooling, networks, and sometimes water in cooling systems. | medium | later |
| 33 | v07-human-centered-ai-ethics | Human-Centered AI Ethics | If AI is powerful, it should decide. | Human at center, AI tools orbiting as instruments, not masters. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Human at center, AI tools orbiting as instruments, not masters. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Human-centered AI ethics asks whether AI systems serve human dignity, learning, responsibility, community, and the common good. | medium | later |
| 34 | v07-risk-myth | Risk vs Myth | The main risk is that the chat model becomes conscious. | Risk and myth sorting board linked to mechanisms. | Sort/check cards by misconception and correction. | Mobile SVG or HTML/SVG hybrid: Risk and myth sorting board linked to mechanisms. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Risk literacy separates practical harms from magical stories about what models are. | low | build now |
| 35 | v07-model-literate-synthesis | Model Literate Synthesis | Either fear or hype is enough to understand AI. | Full prompt-life map with durable/temporary and real/myth overlays. | Tap through numbered steps or reveal labels. | Mobile SVG or HTML/SVG hybrid: Full prompt-life map with durable/temporary and real/myth overlays. | Not justified; use 2D diagram, split screen, or sorting board. | Show final static state with numbered arrows and a concise caption. | Alt/caption should state: Model literacy means explaining what an AI system does, what it does not do, and where practical risks live. | low | build now |
