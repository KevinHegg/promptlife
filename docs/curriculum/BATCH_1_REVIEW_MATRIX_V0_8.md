# Batch 1 Review Matrix v0.8

Scope: first foundation section of the Journey, implemented in app data for v0.8.0.

| Lesson | Definition | Where it happens | Durable vs temporary | Prompt vs response | Metaphor | Brain metaphor | Where it breaks | Visual aid | Checkpoint | Misconception | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| What Is an LLM? | A large language model is a learned system that turns current context into probabilities for the next token. | Whole lifecycle, especially inference from current context to generated response token. | Training/fine-tuning changed weights earlier; inference hidden states are temporary. | Prompt tokens are given; response tokens are generated one at a time. | Vast autocomplete engine with learned structure. | Like using context to anticipate a next word. | People have bodies, goals, lived experience, and awareness; the model has weights and temporary computations. | `llm-overview` | Which description is most accurate? | An LLM is a conscious reader or database. | Implemented |
| Rationalists vs Empiricists | Symbolic AI tries explicit rules and symbols; deep learning learns patterns from data by adjusting weights. | Historical and architectural background. | Deep learning changes weights during training; symbolic rules are written/configured explicitly. | A prompt is processed through learned parameters, not one giant rulebook. | A rulebook versus a trained landscape. | Humans use explicit reasoning and learned intuition. | An LLM is not a full human mind combining logic, experience, emotion, and embodiment. | `traditions` | Which phrase best describes the LLM tradition? | LLMs are hand-coded rulebooks, or symbolic AI is obsolete. | Implemented with existing `history` id preserved |
| Training | Training adjusts model weights by comparing predictions with targets, such as actual next tokens. | Before ordinary use, in a separate optimization process. | Training durably changes weights; ordinary inference does not. | Training examples may resemble prompts/completions, but the system is updating weights. | Practice that changes the instrument before performance. | Like practice shaping future habits or skill. | The model is not reflecting; an optimizer changes numbers. | `training-loop` | What makes training different from inference? | Chatting normally trains the model. | Implemented |
| Pretraining | Pretraining is broad first training where a model learns general patterns by predicting tokens across large datasets. | Before deployment, during foundation-building. | Pretraining durably changes weights. | Pretraining creates weights later used to process prompts and generate responses. | Raindrops carving paths through a landscape. | Like broad education shaping later intuition. | Humans connect learning to lived experience; a model stores statistical structure in parameters. | `pretraining-rain` | During pretraining, what changes? | Pretraining means perfect source recall. | Implemented |
| Overfitting and Generalization | Overfitting learns training examples too narrowly; generalization means learned patterns work beyond seen examples. | During training, evaluation, and model selection. | Overfitting is about what gets installed into weights, not temporary inference. | Generalizing models handle new prompts; overfit models may repeat brittle training patterns. | Studying the answer key versus learning the subject. | Like memorizing practice questions without understanding the principle. | The model is not consciously memorizing; optimization can produce brittle patterns. | `overfitting-generalization` | Which is a sign of overfitting? | Memorization equals understanding. | Implemented as new Journey lesson |
| Fine-Tuning | Fine-tuning is additional training that adapts a pretrained model toward a task, domain, style, or preference. | After pretraining and before or between deployment cycles. | Fine-tuning can durably change weights or adapter behavior; prompting usually steers one run. | Fine-tuning shapes future response patterns rather than generating the current response token. | Carving useful trails through a vast terrain. | Like coaching someone to follow a house style or professional norm. | The model is not choosing a new identity or values; training changes output patterns. | `fine-tune-path` | Which action is closest to fine-tuning? | Prompting once is fine-tuning. | Implemented |
| Alignment | Alignment shapes AI behavior toward human instructions, preferences, safety boundaries, and values. | During fine-tuning, feedback learning, system design, evaluation, deployment, and policy enforcement. | Some alignment changes weights/adapters; some uses prompts, filters, tool rules, or safeguards temporarily. | Alignment affects how the model responds, but not because it understands morality. | Guardrails, coaching, and a compass. | Like learning social norms or professional expectations. | The model does not acquire human values, moral agency, conscience, or care. | `alignment` | What does alignment try to do? | Alignment means moral understanding or guaranteed truth. | Implemented as new Journey lesson |
