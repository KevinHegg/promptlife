# Batch 2 Review Matrix v0.9

Scope: lessons 8-14 in the Journey, implemented in app data for v0.9.0.

| Lesson | Definition | Where it happens | Durable vs temporary | Prompt vs response | Metaphor | Brain metaphor | Where it breaks | Visual aid | Checkpoint | Misconception | Visual layout status | PDF layout status | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Inference | Normal model use: fixed learned weights process current context to produce next-token scores. | Every deployed prompt answer. | Temporary hidden states and scores change; weights normally do not. | Context may include prompt, retrieval, history, and response-so-far; the model generates the next response token. | Using a map without redrawing it. | Like using working memory with current information. | The model computes temporary vectors with fixed weights; it is not conscious or learning. | `inference-pass` | What changes during ordinary inference? | Normal chat automatically trains the model. | HTML legend plus fixed-weight callout. | Passed in v0.9 export. | Implemented |
| Prompt vs Response | Prompt tokens are given; response tokens are generated one at a time and appended. | During inference and response generation. | Prompt and response-so-far are temporary context. | This is the core distinction. | Given cards, then new cards added one at a time. | Like continuing a sentence word by word. | Humans may plan and revise; the model samples next tokens repeatedly. | `prompt-response` | What happens after one response token is generated? | The model generates the whole response at once. | Canonical dog/cat `yard` append example. | Passed in v0.9 export. | Implemented |
| Tokenization | Text is split into chunks that can be represented as token IDs. | Before embedding lookup and transformer layers. | Tokenization does not update weights. | Both prompt and generated response text are represented as tokens. | Parcels on a conveyor belt. | Like hearing language in chunks. | Tokenizer chunks are not human phonemes, memories, or concepts. | `tokenization` | What can a token be? | Tokens are always whole words. | Two-row token layout with simplified-token warning. | Passed in v0.9 export. | Implemented |
| Token IDs | A token ID is the number assigned to a token so the model can look up its starting vector. | Between tokenization and embedding lookup. | The tokenizer/model mapping is fixed during use; using an ID does not update weights. | Prompt and generated response tokens both become IDs. | A library call number. | Like recognizing a symbol and activating an association. | Humans do not assign fixed integer IDs to language chunks. | `token-ids` | Why does the model need token IDs? | The ID is the meaning. | Token, ID, and embedding-row stages separated. | Passed in v0.9 export. | Implemented |
| Embeddings | An embedding is a learned starting vector retrieved for a token ID. | At the start of model processing. | Embedding table is durable; retrieved vector is temporary in the pass. | Prompt and response-so-far tokens are looked up when in context. | Starting address in a meaning cloud. | Like first associations lighting up. | Human associations include memory and sensation; embeddings are rows of numbers. | `embeddings` | What is an embedding in this app? | Embeddings are definitions or memories. | Embedding table, row, and vector bar separated. | Passed in v0.9 export. | Implemented |
| Vectors | A vector is a list of numbers representing features. | Throughout embeddings, hidden states, attention, MLPs, and scoring. | Some vectors are durable parameters; many activations are temporary. | Context tokens become vectors that help produce the next response token. | Dashboard of many sliders. | Like many weak associations lighting together. | Dimensions are learned numerical patterns, not neat labels. | `vectors` | Which statement is most accurate? | Every dimension has a simple English label. | Simplified slider labels plus distributed-feature warning. | Passed in v0.9 export. | Implemented |
| Tensors | A tensor is an organized block of numbers, often holding many token vectors. | Transformer layers and model weights. | Weight tensors are durable; activation tensors are temporary. | Current-context tensor can include prompt, retrieval, history, and response-so-far. | Spreadsheet stack or cube of numbers. | Like keeping many signals active at once. | The brain is not literally a rectangular array. | `tensors` | Which is the better definition of tensor here? | Tensors are mysterious, one word, or one number. | Static tokens x features block; 3D deferred for PDF stability. | Passed in v0.9 export. | Implemented |
